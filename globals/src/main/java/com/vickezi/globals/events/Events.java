package com.vickezi.globals.events;

import com.vickezi.globals.util.CustomValidator;

/**
 * Enum representing different types of events in the system.
 */
public enum Events {
        /**
         * Event indicating that a user has been registered.
         */
        USER_REGISTERED,

        /**
         * Event indicating that a user registration has been received.
         */
        USER_REGISTRATION_RECEIVED;
}
